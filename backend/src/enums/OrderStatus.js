const OrderStatus = Object.freeze({
  PLACED: 'placed',
  PREPARING: 'preparing',
  OUT_FOR_DELIVERY: 'out_for_delivery',
  DELIVERED: 'delivered',
  CANCELLED: 'cancelled',
});

export default OrderStatus;
